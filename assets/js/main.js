$(document).ready(function () {
    //Side nav bar
    $('.button-collapse').sideNav({
        menuWidth:170,
        edge:'left',
        closeOnClick:true,
        draggable:true
        
    });
});